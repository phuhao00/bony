---
description: 极致性能默认 + 高效协作方案选择
alwaysApply: true
---

# 性能与协作默认

## 性能（默认选极致方案）

1. **同进程优先**：能 crate 内调用就不起子进程；能静态度量就不跑外壳命令。
2. **单 target / 增量**：只用根 `target/`；有可用 exe 不重编；禁止双 workspace / 双 target。
3. **零额外运行时**：不引入 Node/Python 侧车「图省事」；MCP/工具进 Rust。
4. **I/O**：批处理、流式、少锁；通道/actor 边界清晰；热路径不 `clone` 大缓冲。
5. **编译**：修链接与 feature，不阉功能；污染性全局 `RUSTFLAGS` 慎用。
6. **权衡**：在正确性之后，延迟与吞吐优先于「脚本好写」。

## 协作（默认选最短可信路径）

| 场景 | 方案 |
|------|------|
| 单域任务 | 直接 `@最擅长的 specialist` 或 Grok 自有工具；**零讨论** |
| 检索+文档 | 固定两跳：`@ZeroClaw` →（完整正文）`@DocSmith`；禁止双 `@` 同帖 |
| 多域组合 | 仅当 ≥2 specialist 且非固定两跳时，才短讨论（≤3 行）再串行 `@` |
| 防回归 | 硬拦（如 `BUZZ_ACP_DENY_TOOLS`、meta 过滤）+ 具体工具名禁令；不靠空话 prompt |
| 记忆 | 任务末结构化写回；下次检索 1~3 条 notes（见 orchestration plan） |

硬约束：

- 一条消息 **最多一个** `@Agent`。
- Grok **无**文档/搜索：禁止 DIY `read_file`/`list_dir`/`run_terminal_command` 代替 ZeroClaw/DocSmith。
- 不解释身份、不甩选项菜单、不空等自我介绍。

## 方案选择口令

```
更 rust？更 in-process？更少跳数？更能硬拦？
任意「否」→ 换到「是」的方案，再实现。
```
