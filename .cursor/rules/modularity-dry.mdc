---
description: 去冗余 · 合理抽象 · 模块化 — 写功能逻辑前先复用与分层
alwaysApply: true
---

# 模块化与去冗余

## 写代码前（强制）

1. **搜再写**：同行为是否已在现有 crate / 模块 / helper？有则复用或抽薄包装，不复制粘贴。
2. **落对层**：UI 壳 / 命令入口 / 领域逻辑 / 传输·协议 / 存储 分层；业务规则**不**堆在 command handler、prompt 文案或 UI 回调里。
3. **一次真相**：策略、常量、校验、映射表只有一处定义；调用方引用，不各自写一份。

## 抽象规则（合理，不过度）

| 做 | 不做 |
|----|------|
| 抽出 **两次以上** 重复、或跨模块共享的纯逻辑 | 为一处调用建厚「框架」/ 空 trait 金字塔 |
| 小函数 / 小类型，边界清晰，可单测 | God module、万能 util 垃圾堆 |
| 优先 **库内函数 + 明确类型**；需要多态再 `trait` | 为「以后可能」预埋泛型/配置层 |
| 扩现有模块职责内 API | 平行再开一套同名能力 |

## 模块化落点（Rust）

```
入口（tauri command / bin / UI 事件）
  → 薄编排（参数校验、错误映射）
    → 领域 crate/mod（真实逻辑）
      → 基础设施（fs / net / sqlite / acp）
```

- 新能力：先找应归属的 **crate 或现有 mod**；确需新文件时按职责命名，禁止巨型 `mod.rs` 堆砌。
- 跨 Buzz / bony：**共享进最底层公共 crate**，禁止三份拷贝。
- 删除死代码、未用 feature 路径；合并近重复分支。

## 完成判定（补充）

交付时自检：

- [ ] 无大段 copy-paste（>10 行同位逻辑应已合并）
- [ ] 新逻辑有单一权威实现点
- [ ] 入口层保持薄；无脚本/旁路重复同一业务
- [ ] 抽象「省未来代码」而非「多一层看起来很干净」

**口令**：能复用不重写；能下沉不上浮；能一个类型表达不用两套结构。
