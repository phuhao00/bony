# Workspace root for the Bony / Buzz product.

[patch.crates-io]
# Temporary fork pin: aws-creds 0.39.1 (via rust-s3) cannot read EKS Pod Identity
# credentials. Revert to crates.io once durch/rust-s3#449 lands upstream.
aws-creds = { git = "https://github.com/tlongwell-block/rust-s3", rev = "c9fce3620dd434c1f810101d672cf384268dbb0f" }

[workspace]
resolver = "2"
members = [
    # Buzz product (first-class members)
    "crates/buzz-relay",
    "crates/buzz-core",
    "crates/buzz-conformance",
    "crates/buzz-push-gateway",
    "crates/buzz-db",
    "crates/buzz-pubsub",
    "crates/buzz-auth",
    "crates/buzz-search",
    "crates/buzz-audit",
    "crates/buzz-acp",
    "crates/buzz-agent",
    "crates/sprig",
    "crates/buzz-test-client",
    "crates/buzz-ws-client",
    "crates/buzz-admin",
    "crates/buzz-workflow",
    "crates/buzz-media",
    "crates/buzz-cli",
    "crates/buzz-pairing-cli",
    "crates/buzz-sdk",
    "crates/buzz-persona",
    "crates/git-credential-nostr",
    "crates/git-sign-nostr",
    "crates/buzz-pair-relay",
    "crates/buzz-relay-mesh",
    "crates/buzz-dev-mcp",
    "crates/buzz-economy",
    "crates/buzz-voice",
    "desktop/src-tauri",
    "desktop/src-tauri/crates/buzz-terminal",
]

[workspace.package]
edition = "2024"
license = "Apache-2.0"

[workspace.dependencies]
agent-client-protocol = { version = "0.10.4", features = ["unstable"] }
alacritty_terminal = "0.26.0"
ansi-to-tui = "7.0.0"
ansi-width = "0.1"
anstyle = "1.0"
anstyle-lossy = "1.1.4"
anstyle-parse = "0.2"
anstyle-syntect = "1.0.4"
anyhow = { version = "1", features = ["backtrace"] }
arboard = "3.6"
arc-swap = "1.7"
assert_matches = "1.5.0"
async-compression = { version = "0.4.17", default-features = false, features = ["gzip", "zstd", "tokio"] }
async-lsp = { version = "0.2.3", features = ["tokio", "tracing"] }
async-openai = { version = "0.33.0", features = ["responses"] }
async-stream = "0.3"
async-trait = "0.1"
axum = { version = "0.8", features = ["macros", "ws"] }
backon = { version = "1.6" }
backtrace = "0.3"
base64 = "0.22"
blake3 = "1"
bytes = { version = "1", features = ["serde"] }
camino = "1.1.10"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4", features = ["derive", "env"] }
clap_complete = { version = "4" }
colored_json = "5"
core-foundation = "0.10"
crc32fast = "1.4"
criterion = "0.6"
crossbeam = "0.8"
crossterm = "0.28"
cryptify = "3.2"
ctor = "0.4"
dashmap = "6"
derive_more = { version = "2", features = ["add", "add_assign", "debug", "deref", "deref_mut", "display", "from", "from_str", "into", "into_iterator", "try_into"] }
dhat = "0.3"
dirs = "5.0"
documented = "0.9"
dunce = "1"
educe = "0.6.0"
encoding_rs = "0.8.35"
enum_delegate = "0.2"
env_logger = "0.11"
eventsource-stream = "0.2"
fastrace = { version = "0.7" }
fastrace-opentelemetry = { version = "0.18" }
fastrace-reqwest = { version = "0.2" }
fastrace-tonic = { version = "0.1" }
fastrand = "2"
filetime = "0.2.25"
flate2 = { version = "1", default-features = false, features = ["zlib-rs"] }
fontdb = "0.23"
fs2 = "0.4"
futures = "0.3"
futures-util = "0.3"
fuzzy-matcher = "0.3.7"
gcloud-storage = { version = "1.3.0", default-features = false, features = ["auth", "jwt-aws-lc-rs", "external-account"] }
gethostname = "1"
gix = { version = "0.83", default-features = false, features = ["sha1"] }
glob = "0.3"
globset = "0.4"
htmd = "0.5.4"
html-escape = "0.2"
http = "1"
http-body-util = "0.1"
humantime-serde = "1"
ignore = "0.4"
image = { version = "0.25.10", default-features = false }
indexmap = { version = "2", features = ["serde"] }
indicatif = "0.18"
infer = "0.19.0"
insta = "1"
itertools = "0.14"
jsonschema = "0.30.0"
kamadak-exif = "0.5"
libc = "0.2"
linkify = "0.10"
lipsum = "0.9"
log = { version = "0.4", features = ["kv", "kv_unstable"] }
md5 = "0.8"
minijinja = "2.9"
mockito = "1"
moka = { version = "0.12", features = ["sync"] }
nix = { version = "0.30", features = ["poll", "process", "signal", "sched", "term", "mount", "fs", "ioctl", "mman", "reboot", "user"] }
notify = "8"
notify-debouncer-mini = "0.6"
nucleo = { git = "https://github.com/helix-editor/nucleo.git", rev = "5b74652" }
num_cpus = "1"
oauth2 = "5"
obfstr = "0.4"
once_cell = "1"
opentelemetry = { version = "0.32", features = ["trace"] }
opentelemetry-http = { version = "0.32", features = ["reqwest-blocking"] }
opentelemetry-otlp = { version = "0.32", default-features = false, features = ["trace", "grpc-tonic", "reqwest-blocking-client", "tls-roots", "tls-ring"] }
opentelemetry-proto = { version = "0.32", features = ["gen-tonic"] }
opentelemetry_sdk = { version = "0.32.1", features = ["trace", "rt-tokio", "spec_unstable_metrics_views"] }
parking_lot = "0.12.4"
pbjson-build = "0.9"
pdf_oxide = { version = "0.3.43", features = ["rendering"] }
calamine = "0.26"
rust_xlsxwriter = "0.79"
docx-rs = "0.4"
printpdf = "0.7"
petgraph = { version = "0.6.5", default-features = false, features = ["serde-1", "stable_graph"] }
portable-pty = "0.9"
pprof = "0.15"
pretty_assertions = "1"
prometheus = { version = "0.14", features = ["process"] }
prost = "0.14"
prost-build = "0.14"
prost-reflect = "0.16"
pulldown-cmark = "0.13"
quick-xml = "0.38"
rand = "0.9"
ratatui = { version = "0.29" }
ratatui-core = "0.1"
rayon = "1"
rcgen = { version = "0.13", features = ["pem"] }
reflink-copy = "0.1"
regex = "1"
reqwest = { version = "0.12", features = ["rustls-tls", "stream", "json", "multipart", "http2", "blocking", "socks"], default-features = false }
reqwest-middleware = { version = "0.4.1", features = ["json", "multipart"] }
resvg = { version = "0.47", default-features = false, features = ["text"] }
rhai = { version = "1.25", features = ["serde"] }
ring = "0.17"
rsa = "0.9"
runfiles = "0.1"
rustc-hash = "2"
rustls = { version = "0.23", features = ["aws-lc-rs"] }
schemars = "1"
scraper = "0.23"
semver = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_path_to_error = "0.1"
serde_yaml = "0.9"
serial_test = "3"
sha1 = "0.10"
sha2 = { version = "0.10", features = ["force-soft"] }
shlex = "1"
signal-hook = "0.3"
similar = "2.7"
siphasher = "1"
smallvec = "1"
strip-ansi-escapes = "0.2.1"
strum = { version = "0.27", features = ["derive"] }
supports-color = "3.0"
syntect = "5.3"
tar = "0.4"
tempfile = "3"
terminput = "0.3"
termwiz = { version = "0.23", default-features = false }
textwrap = "0.16"
thiserror = "2"
tikv-jemalloc-ctl = "0.6"
tikv-jemalloc-sys = "0.6"
tikv-jemallocator = { version = "0.6", features = ["profiling", "unprefixed_malloc_on_supported_platforms"] }
time = "0.3"
tiny-skia = "0.12"
tokio = { version = "1", features = ["full"] }
tokio-retry = "0.3"
tokio-stream = { version = "0.1", features = ["net"] }
tokio-tungstenite = "0.27"
tokio-util = { version = "0.7", features = ["rt"] }
toml = "0.9"
toml_edit = "0.22"
tonic = { version = "0.14", features = ["tls-aws-lc", "gzip", "deflate", "zstd"] }
tonic-prost = "0.14"
tonic-prost-build = "0.14"
tower = { version = "0.5", features = ["timeout", "util", "limit"] }
tower-http = { version = "0.6", features = ["trace", "cors", "compression-gzip", "limit", "timeout", "fs"] }
tracing = "0.1"
tracing-opentelemetry = "0.33"
tracing-subscriber = { version = "0.3.23", default-features = false, features = ["env-filter", "smallvec", "tracing-log", "std", "ansi", "json"] }
ts-rs = "12.0"
tui-scrollbar = "0.2"
two-face = { version = "0.4", default-features = false, features = ["syntect-fancy"] }
unicode-normalization = "0.1"
unicode-segmentation = "1.12.0"
unicode-width = "0.2"
url = "2"
urlencoding = "2"
uuid = { version = "1", features = ["serde", "v4", "v5"] }
vte = "0.15.0"
wait-timeout = "0.2"
walkdir = "2"
webbrowser = { version = "1.0.4" }
webpki-roots = "0.26"
which = "8"
whoami = "1.4"
wildmatch = "2"
windows = { version = "0.61", features = ["Win32_Security", "Win32_Security_Authorization", "Win32_Foundation", "Win32_System_Threading", "Win32_System_JobObjects", "Win32_System_Console", "Win32_System_Pipes"] }
wiremock = "0.6"
wl-clipboard-rs = "0.9"
zbus = { version = "5" }
zip = "3"
zstd = "0.13"

# --- Buzz stack (shared workspace deps; version conflicts stay as literals in Buzz crates) ---
sqlx = { version = "0.9", features = ["runtime-tokio", "tls-rustls", "sqlite", "uuid", "chrono", "json"] }
redis = { version = "1.0", features = ["tokio-comp", "connection-manager", "tokio-rustls-comp"] }
deadpool-redis = { version = "0.23", features = ["rt_tokio_1"] }
nostr = { version = "0.44", features = ["nip44", "nip98"] }
postcard = { version = "1", default-features = false, features = ["use-std"] }
iroh = { version = "1.0.0-rc.0", default-features = false, features = ["tls-ring"] }
evalexpr = "11"
cron = "0.16"
metrics = "0.24"
metrics-util = "0.20"
hex = "0.4"
hmac = "0.13"
subtle = "2.6"
zeroize = "1.8"
proptest = "1"
rmcp = { version = "1.1.0", features = ["server", "transport-io", "macros"] }
# Ensure shared deps cover Buzz feature needs (union with existing)
# note: tokio/axum/tower already declared above — crate-level feature activation still works
# buzz-search vector (semantic) search backend — embedded, no server process.
lancedb = "0.31.0"
arrow-array = "58.0.0"
arrow-schema = "58.0.0"

buzz-core = { path = "crates/buzz-core" }
buzz-conformance = { path = "crates/buzz-conformance" }
buzz-db = { path = "crates/buzz-db" }
buzz-auth = { path = "crates/buzz-auth" }
buzz-pubsub = { path = "crates/buzz-pubsub" }
buzz-search = { path = "crates/buzz-search" }
buzz-audit = { path = "crates/buzz-audit" }
buzz-workflow = { path = "crates/buzz-workflow" }
buzz-media = { path = "crates/buzz-media" }
buzz-sdk = { path = "crates/buzz-sdk" }
buzz-ws-client = { path = "crates/buzz-ws-client" }
buzz-relay-mesh = { path = "crates/buzz-relay-mesh" }
buzz-economy = { path = "crates/buzz-economy" }

[profile.release]
incremental = true
panic = "abort"

# CI profile — was in buzz workspace (relay desktop e2e).
[profile.ci]
inherits = "release"
lto = false
opt-level = 1

[profile.ci.package."*"]
opt-level = 3

# Sprig size-focused release profile (was in buzz).
[profile.sprig]
inherits = "release"
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

# Hardened release profile for shipping to end users (alpha + stable).
# Use `cargo build --profile release-dist` for distribution builds.
# Not used by default `--release` to avoid slowing down local dev builds (~2.2x).
#
# Hardening: thin LTO for cross-crate optimization with reasonable link times,
# codegen-units=1 maximizes optimization (whole-program analysis, no CGU boundaries).
# debug=1 emits line-tables DWARF; strip=false keeps symbols so CI can extract
# .debug sidecars (Linux) or .dSYM bundles (macOS) before stripping post-build.
# Linker flags (RELRO, NX stack) are set per-target in .cargo/config.toml.
[profile.release-dist]
inherits = "release"
lto = "thin"
codegen-units = 1
strip = false
debug = 1
split-debuginfo = "off"

# Production profile for latency-sensitive x-product services (VF, home-mixer).
# Thin LTO gives ~90% of full LTO benefit at significantly faster link time.
# Keeps symbols + line tables for prod debuggability (perf, flamegraph, stack traces).
[profile.x-prod]
inherits = "release"
lto = "thin"
strip = false
codegen-units = 1
debug = "line-tables-only"
panic = "unwind"

# Desktop release profile. Functionally identical to release-dist — kept as a
# named alias so the desktop workflow can reference it without coupling to the
# CLI pipeline's profile name. Alpha and stable share a single release-dist
# profile so that stable promotion can pointer-swap the alpha binary without
# rebuilding.
[profile.release-dist-jemalloc]
inherits = "release-dist"
# All settings now match the parent — this is an intentional alias.



[profile.bench]
debug = true

[workspace.lints.clippy]
# prost 0.14 renders proto doc-comment bullet lists in a way that trips this
# lint on generated code. Kept in sync with bazel/lint/linters.bzl.
doc_lazy_continuation = "allow"
doc_overindented_list_items = "allow"
needless_lifetimes = "allow"
single_range_in_vec_init = "allow"
too_many_arguments = "allow"

# Allow uninlined_format_args lint to prevent main from breaking after merges
# When older branches (created before this lint was added to CI) get merged,
# they pass their own CI because their CI config doesn't include this lint check.
# However, once merged to main, the code violates main's lint rules, causing:
# 1. Main's CI to fail
# 2. All new branches based off main to fail CI
# 3. Developers having to fix lint violations from other people's merged code
# Without a merge-queue that runs main's CI checks on the PR branch,
# these violations slip through and break the build for everyone.
# TODO: -> "deny" once/if merge queue enabled
uninlined_format_args = "allow"

# The `fastrace::trace(properties = { … })` proc-macro expands `"{param}"`
# into `format!("{}", param)`, which clippy flags as useless_format.
# Suppressed until the upstream crate fixes its codegen (fixed in 0.7.16+).
useless_format = "allow"
