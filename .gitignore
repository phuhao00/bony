/target
**/*.rs.bk
.DS_Store

# Local packaging / release artifacts (never commit binaries)
.local-dist/
/dist/

# Local dumps / relay uploads
/RESEARCH/
/uploads/

# Frontend
/node_modules/
/desktop/node_modules/
/desktop/dist/
/desktop/**/.turbo/

# Local relay config and SQLite (never commit)
.env
/buzz.db
/buzz.db-wal
/buzz.db-shm

# Leftover vendor path if a locked folder could not be moved
/third_party/
/_nm_stash/

# Local agent smoke / relay logs
/tmp-*.log
/tmp-*.err
/tmp-*.out
/tmp-*.diff
tmp-*.log
tmp-*.out
tmp-*.err
tmp-buzz*.diff
tmp-discovery*.diff

# Local runtime PIDs/logs for room stack
.local-runtime/
